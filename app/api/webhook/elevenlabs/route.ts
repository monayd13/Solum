// Backward-compatible alias for existing ElevenLabs webhook configuration.
// Both routes share the same fail-closed signature verification and processor.
export { GET, POST } from "../post-call/route";
